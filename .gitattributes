# Normalise line endings: LF in the repo, LF on checkout everywhere except
# Windows batch files, which must stay CRLF.
* text=auto eol=lf
*.bat text eol=crlf
*.png binary
*.jpg binary
*.jpeg binary
*.woff2 binary
*.zip binary
